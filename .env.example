# 보안영역 내부 PostgreSQL 접속 정보 (실제 값은 .env 에만 설정, Git에 커밋하지 않음)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/workorder?schema=public"

# openssl rand -base64 32 로 생성
NEXTAUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:4200"

PORT=4200
