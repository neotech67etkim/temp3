# 이 서버 로컬 디스크에 SQLite 데이터 파일들(org.db, 과별 .db)을 저장할 폴더.
# 서버가 재시작/재부팅돼도 그대로 있어야 하는 경로를 지정하세요.
DATA_DIR="./.dev-data"

# openssl rand -base64 32 로 생성
NEXTAUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:4200"

PORT=4200
